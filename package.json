{
  "dependencies": {
    "express": "^4.21.1",
    "express-handlebars": "^8.0.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "buildTemplate": "handlebars views/partials/dataCard.handlebars -f public/dataCardTemplate.js",
    "prestart": "npm run build"
  }
}
